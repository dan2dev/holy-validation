import Store from "./store";
import './common';
export declare module HolyValidation {
    const register: typeof Store.register;
    const execute: typeof Store.execute;
}
export default HolyValidation;
